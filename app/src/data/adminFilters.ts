// Shared filter vocab for Admin Modules (Template Builder field palette +
// formula picker). Hardcoded for now — swap for a live master-data fetch
// once the Master Data module is wired to real DB tables.

export const ALL_COMPANIES = 'All Companies';
export const ALL_PRODUCTS = 'All Products';
export const ALL_TEMPLATES = 'All Templates';

export const COMPANY_OPTIONS = ['ADNOC', 'ENOC', 'Grey Market'];
export const PRODUCT_OPTIONS = ['Diesel', 'Gasoline'];
